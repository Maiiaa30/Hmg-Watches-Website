The base surface — charcoal with a hairline border. `interactive` adds a subtle hover lift and gold border for clickable cards.

```jsx
<Card>Conteúdo</Card>
<Card interactive>Card clicável</Card>
```
