A square hairline checkbox that fills gold when checked. Used for catalogue filters (brand, movement type).

```jsx
<Checkbox label="Rolex" checked={on} onChange={e => setOn(e.target.checked)} />
```
